[pytest]
asyncio_mode = auto
testpaths = easyinsta/tests
python_files = test_*.py *_tests.py
filterwarnings =
    ignore::DeprecationWarning
addopts =
    --cov=easyinsta
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail
